%h1 Candidates
= link_to 'New task', new_task_path
%table.table
  %thead
    %tr
      %th{scope: 'col'} Candidate name
      %th{scope: 'col'} Task count
  %tbody
    - @candidates.each do |candidate|
      %tr
        %td= link_to candidate.name, candidate_path(candidate)
        %td= Task.where(candidate_ids: [candidate.id]).size
