.nested-fields
  = f.hidden_field :_destroy
  %div
    = f.label 'Candidate'
    = f.select :candidate_id, Candidate.all.collect {|c| [ c.name, c.id ] }, { include_blank: true })
  %div
    = link_to "Remove", '#', class: "remove_fields"